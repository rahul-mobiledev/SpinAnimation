<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SpinAnimation" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SpinAnimation" parent="Base.Theme.SpinAnimation" />

    <style name="SpinText">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>